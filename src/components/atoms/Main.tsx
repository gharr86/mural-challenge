import styled from 'styled-components';

const Main = styled.main`
  display: flex;
  flex-direction: column;
  width: 100vw;
  min-height: 100vh;
  margin: 0;
  padding-bottom: 3rem;
  font-family:
    Inter,
    system-ui,
    -apple-system,
    sans-serif;
`;

export default Main;
