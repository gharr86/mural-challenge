import styled from 'styled-components';

const Main = styled.main`
  width: min(720px, 100% - 2rem);
  margin: 3rem auto;
  font-family:
    Inter,
    system-ui,
    -apple-system,
    sans-serif;
`;

export default Main;
